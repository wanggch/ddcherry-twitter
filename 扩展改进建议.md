# X Post Saver（ddcherry-twitter）扩展改进建议

> 视角：资深 Chrome 扩展工程师 + 运营  
> 编写日期：2026-08-12  
> 范围：本仓库当前实现（结合 `src/`、`manifest`、配套 `x-likes-to-obsidian`）  
> 原则：不空谈，按优先级：**体验与稳定性 > 产品清晰度 > 工程化 > 上架与增长**

---

## 总判断

这是一条**个人知识工作流工具**（抓 X → 剪贴板 / Obsidian），主路径（图标一点 → Markdown + 可选 18787）方向对、闭环已通。

当前更像「功能叠出来的工具」，还不是「可长期用、可交付别人用的产品」：

- **多条保存出口**（剪贴板 / 18787 Obsidian / 27123 笔记 / 远程 API）叙事不清  
- **主交互隐藏在无 Popup 的图标点击里**，失败难诊断  
- **DOM 解析**是最大单点风险，却几乎没有「等页面就绪 / 降级 / 自检」  
- 权限与文案仍停在「Save to backend」时代，和真实用法脱节  

---

## 一、产品与交互（运营感最强）

### 1. 先定「默认一键路径」，其余进二级

| 现状 | 问题 |
|------|------|
| 图标点击：剪贴板 + 可选 Obsidian | 主路径 OK，但用户不知道发生了什么 |
| Popup：远程 API + 27123 | **manifest 未挂 `default_popup`，代码在却默认进不去** |
| 右键：另一套逻辑 | 与图标行为不完全一致 |

**建议**

- **默认产品定义一句话**：「在 X 上点一下，内容进剪贴板；若开了 Obsidian，再同步进库。」  
- 远程 API、27123 标成「高级 / 可选」，选项里分组，不要和主路径平级。  
- 二选一产品形态：  
  - **A. 极简（推荐自用）**：继续无 Popup，靠 Toast + 通知 + 图标角标反馈。  
  - **B. 可运营**：`default_popup` 做「预览 + 一键保存 + 最近结果」，图标点击只打开 Popup，避免「点了不知道成没成」。

个人向工具选 A 即可；若给同事/读者装，选 B。

### 2. 反馈要做到「3 秒内可理解」

建议补齐：

| 反馈 | 作用 |
|------|------|
| 工具栏 **badge**（✓ / ! / …） | 不依赖页面 Toast 是否被 X 盖住 |
| 系统 **notification** 文案中文化、可点开选项 | 已有 `notifications` 权限，应用好 |
| 失败分型：「不是帖子页 / 页面未加载完 / 本地服务没开 / 解析失败」 | 现在多是泛化中英文混杂 |
| 成功后 badge 显示 2～3 秒再清 | 降低「点了没反应」客诉 |

### 3. 中英文与文案体系统一

Popup / Options 中英混用（`Save Settings` vs「保存到本地」），`description` 还写 *backend API*，和主卖点 Obsidian 不符。

**运营向**：面向中文用户就全中文；若将来上架国际版，再做 `_locales`（Chrome i18n）。展示名可考虑更贴场景，例如「X 收藏到 Obsidian」，比中性的 X Post Saver 好记、好搜。

### 4. 选项页做成「工作流向导」

现在像工程师配置表。建议：

1. 你要用哪种模式？（仅剪贴板 / 剪贴板+Obsidian / 远程 API）  
2. 测连通：`GET /api/health` 绿/红  
3. 保存后给一句「去任意帖子页点图标试一次」  

第一次成功体验，决定这个扩展会不会被关掉。

---

## 二、可靠性（扩展工程师最关心）

### 5. DOM 解析：必须「等就绪 + 重试 + 可热修」

X 是 SPA，刷新后 `article[data-testid="tweet"]` 常晚几百毫秒才出现；当前基本是**点一下立刻 parse**，失败率会周期性升高。

**建议（P0）**

- content script 内：`waitForSelector`（超时 3～5s，轮询或 `MutationObserver`）  
- 失败自动重试 2～3 次（间隔 300～500ms）  
- 多主推 + 引用推时，明确「取哪一条」（当前 URL 的 status id 对应 article，不要 silently 取页面第一篇）  
- 选择器表集中配置（已有雏形），并预留 **远程选择器配置**（storage 或小 JSON）：X 改版时可不发版热修  

### 6. 线程 / 长文 / 媒体的边界写进产品

| 能力 | 建议 |
|------|------|
| Thread | 至少「当前帖 + 是否为 thread 一部分」标记；P1 再做合并全文 |
| 长文章 | 已做；定期用真实 Article URL 做回归清单 |
| 图片 | 现只存 URL；Obsidian 侧若只转写文字，文档写清「图需另下」或后续接本地下载 |
| 视频 / 卡片 / 投票 | 明确「不支持」，避免用户以为丢了 |

### 7. 本地服务挂了要有「队列」，别只 Toast 一句失败

Obsidian 关着时：剪贴板已成功，但库没进——用户隔天找不到。

**建议**

- `chrome.storage.local` 做 **pending 队列**（最近 N 条）  
- 服务恢复后（定时 `alarms` 探活，或下次点击时 flush）自动重试  
- 选项页显示「待同步 3 条」  

这是个人工具里**投入产出比最高**的可靠性功能之一。

### 8. Service Worker 生命周期

MV3 SW 随时休眠：右键菜单在 `onInstalled` 建一次，部分更新场景会丢。

**建议**：`onInstalled` + `onStartup` 都 `create` 菜单（或先 `removeAll` 再建）；关键逻辑别假设 SW 长驻内存。

### 9. Popup 与图标路径逻辑统一

| 入口 | 当前行为 |
|------|----------|
| 图标点击 | 剪贴板 + 可选 Obsidian |
| 右键 | API 或剪贴板，**不走 Obsidian** |
| Popup | 远程 / 27123 |

用户会问：「为啥右键没进库？」——应统一编排一个 `savePipeline(content, triggers)`，各入口只传触发源。

---

## 三、权限、安全、合规

### 10. 最小权限（上架与用户信任）

| 权限 | 现状 | 建议 |
|------|------|------|
| `tabs` | 有 | 多数场景 `activeTab` + 消息即可，评估能否去掉 |
| `notifications` | 有 | 用则保留，否则删 |
| `clipboardWrite` | 有 | 剪贴板主路径需要；文档写清用途 |
| `host_permissions` 写死 18787/27123 | 有 | 改 **optional_host_permissions** 或选项里动态请求；远程 `apiUrl` 更应 optional |
| `x.com/*` 全站 content script | 有 | 合理；可 `run_at: document_idle`，减少首页无谓开销 |

硬编码 localhost 端口：**换端口就要发版**。更好：选项可配 baseUrl，并在首次保存时 `permissions.request`。

### 11. 安全与隐私（个人工具也要写清楚）

- 内容含私密帖级敏感信息时，本地服务明文 POST：限制 `127.0.0.1`、文档强调勿绑定 `0.0.0.0`。  
- 远程 API Key 存 `storage.sync`：会同步到 Google 账号；敏感 key 更适合 `storage.local`。  
- 若未来上架：需要**隐私权政策**一页（收集什么、是否出网、是否卖数据）。当前若纯本地，就写「默认不出网，仅用户配置的 URL」。

### 12. CSP 与构建产物

Vite 多段 IIFE 构建可用，但 `closeBundle` 二次 `build` 偏脆。长期可迁 **CRXJS / WXT / Plasmo** 一类扩展脚手架，少踩「HTML 路径、SW、content script 分包」坑。  
检查 `dist` 是否误带 source map / 测试文件。

---

## 四、工程化与可维护性

### 13. 测试缺口

目前几乎只有 `obsidianPayload` 单测。建议：

| 类型 | 内容 |
|------|------|
| 单元 | `urlParser`、Markdown 格式、错误码映射 |
| Fixture | 保存若干真实（脱敏）HTML 片段，测 extractor |
| 手动清单 | 帖子 / 引用 / 长文 / 未登录墙 / 服务关闭 — 装前 5 分钟回归 |

DOM 全自动 e2e 成本高，**HTML fixture 回归**最划算。

### 14. 版本与变更

长期停在 `1.0.0`。建议：

- semver：`1.1.0` 功能，`1.0.1` 选择器热修  
- `CHANGELOG.md` 三行也够  
- manifest `version` 与 npm `version` 同源（CI 或脚本同步）

### 15. 可观测性（自用也要）

选项页加「调试模式」：

- 最近一次提取的 JSON（可复制）  
- 最近错误码与时间  
- 选择器版本号  

X 一改版，能 30 秒判断是「解析挂了」还是「服务挂了」。

### 16. 死代码与双端口叙事

- Popup 与 27123：要么产品化接好，要么标注实验/移除，避免文档三套说法。  
- 27123 与主链路 18787 继续严格分开（产品 UI 上要同步文档分层）。

---

## 五、运营与分发

### 17. 分发策略先想清

| 模式 | 做法 |
|------|------|
| **仅自己** | 加载 `dist` + 安装指南；打 zip 备份；不必上架 |
| **小范围好友** | 提供「一键 zip + 截图 5 步」；可选 Edge 加载相同包 |
| **公开上架** | 商店图、隐私政策、单一清晰价值主张；localhost 能力要讲清「需自备本地服务」 |

若上架 CWS：**强依赖用户自建 Rust 服务**会劝退大多数人。公开版卖点应是「复制为 Markdown / 导出笔记」；Obsidian 作 Power User 开关。

### 18. 内容资产（运营）

- 30 秒演示 GIF：帖子页 → 点图标 → Obsidian 出现文件  
- 故障 FAQ：服务未启动、不是详情页、解析失败刷新再试  
- 与 `x-likes-to-obsidian` **版本兼容表**（扩展 1.x 要求服务 ≥ 某版本）

### 19. 命名与品牌

`ddcherry-twitter` / `x-post-saver` / `X Post Saver` 三套名字。对内仓库名可保留，对外统一一个展示名 + 一句 slogan，减少「我装的是哪个」混乱。

---

## 六、功能路线图（建议排期）

### P0（1～2 周，体感立刻变好）

1. 提取：wait + retry + 按 status id 对齐主帖  
2. 统一三条入口的保存管线  
3. Badge / 中文通知 + 错误分型  
4. 选项页「检测本地服务」按钮  
5. 文案与 README 对齐真实主路径  

### P1（增强粘性）

6. 失败/离线队列自动重试  
7. 恢复或重构 Popup：预览 + 主按钮「复制并同步」  
8. HTML fixture 测试 + 选择器版本号  
9. optional 权限与可配置 baseUrl  
10. Thread 基础支持  

### P2（产品化 / 上架）

11. 迁 WXT/Plasmo，规范构建  
12. i18n、商店素材、隐私页  
13. 远程选择器热更新  
14. 图片本地下载进 vault（与 Rust 侧协作）  

---

## 七、若只能做三件事

站在「自己天天用、偶尔帮人装」的现实：

1. **解析 wait/retry + 主帖对齐** —— 少踩 X 改版与 SPA 空窗。  
2. **本地服务离线队列** —— 真正确保「点过的都进库」。  
3. **反馈与选项探活中文化** —— 降低「坏了却不知道哪里坏」的维护成本。  

---

## 八、一句话产品建议

把对外叙事从 *「Save posts to your backend API」* 改成：

> **在 X 上一点，Markdown 进剪贴板；可选自动进 Obsidian。**

工程上围绕这条主路径收口权限、交互、错误处理与文档；其余能力当插件，而不是并列主角。

---

## 相关文档

| 文档 | 说明 |
|------|------|
| [README.md](./README.md) | 项目入口 |
| [项目梳理与文档规范.md](./项目梳理与文档规范.md) | 全貌与文档原则 |
| [docs/安装指南.md](./docs/安装指南.md) | 扩展 + 本地服务安装 |

---

*本文为改进意见备忘，非实现承诺；落地时可按 P0 拆 PR。*
