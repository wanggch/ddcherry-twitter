# 项目梳理与文档规范

> 编写日期：2026-08-12  
> 范围：本仓库 `ddcherry-twitter`（扩展）及与其配套的本地服务关系  
> 目的：久未维护后快速回神；统一文档放哪里、叫什么、何时改

---

## 一、一句话说明

**这是一个 Chrome/Edge 扩展（X Post Saver）**：在 X/Twitter 的帖子或长文章页提取内容，默认复制成 Markdown；可选推到自定义后端，或推到本地 Rust 服务再写入 Obsidian。

npm 包名 / 目录名：`x-post-saver` / `ddcherry-twitter`  
扩展显示名：`X Post Saver`（`public/manifest.json`）

---

## 二、它在整条链路里的位置

本仓库**只做浏览器端采集与转发**，不负责写磁盘、不负责大模型转写。

```text
┌─────────────────────┐     剪贴板 Markdown
│  X / Twitter 页面   │ ──────────────────────► 用户粘贴
└──────────┬──────────┘
           │ content-script 提取
           ▼
┌─────────────────────┐
│  本扩展 (本仓库)     │
│  · 图标点击          │──► POST /api/tweets/import ──► x-likes-to-obsidian (18787)
│  · 右键菜单          │     去重 / DeepSeek 转写 / 写 vault
│  · Popup            │──► 自定义 apiUrl（可选远程后台）
│  · Options          │──► Popup 旁路：POST /api/note (27123 本地笔记服务)
└─────────────────────┘
```

| 组件 | 位置 | 端口/入口 | 职责 |
|------|------|-----------|------|
| 浏览器扩展 | **本仓库** | `dist/` 加载 | DOM 提取、剪贴板、调用下游 |
| x-likes-to-obsidian | 同级 `../x-likes-to-obsidian` | `127.0.0.1:18787` | 导入、SQLite 去重、DeepSeek 转写、写 Obsidian |
| 本地笔记服务 | **不在本仓库** | `127.0.0.1:27123` | 简单按 JSON 生成 Markdown 文件（Popup 用） |

**主推荐日常路径**：扩展图标 → 剪贴板 +（可选）18787 Obsidian。  
**旁路路径**：Popup 里对 27123 的本地笔记保存（需另有服务实现）。

---

## 三、核心能力（对照代码）

### 3.1 已实现

| 能力 | 主要代码 | 说明 |
|------|----------|------|
| 帖子页识别与提取 | `urlParser.ts`, `postExtractor.ts` | `…/status/{id}` |
| 长文章识别与提取 | `articleExtractor.ts` | `…/i/article/{id}`，DOM + meta 降级 |
| 图标一点即用 | `iconClickHandler.ts` | 先剪贴板，再可选 Obsidian |
| 右键菜单 | `serviceWorker.ts` | 帖子 / 文章分菜单项 |
| Popup 预览与保存 | `popup/` | 远程 API 或剪贴板；另有 27123 本地保存 |
| 配置页 | `options/` | apiUrl / apiKey / enableObsidian / obsidianBaseUrl |
| Markdown 格式化 | `markdownFormatter.ts` | 剪贴板内容 |
| Obsidian 请求体 | `obsidianPayload.ts` | `tweetId, text, author…` |
| 远程 API 保存 | `apiClient.ts` | Bearer Token 可选 |
| 页面 Toast | `toast.ts` / `toastUtils.ts` | 成功/失败反馈 |
| 单测 | `obsidianPayload.test.ts` 等 | `npm test` |

### 3.2 配置模型

```ts
// src/common/types.ts — ExtensionConfig
{
  apiUrl: string;           // 空 = 远程保存关闭，走剪贴板逻辑
  apiKey: string;
  enableObsidian: boolean;  // 仅影响图标点击后是否调 18787
  obsidianBaseUrl: string;  // 默认 http://127.0.0.1:18787
}
```

存储：`chrome.storage.sync`，key = `extensionConfig`。

### 3.3 数据模型（简）

- `PostData`：`postId, url, content, images[], author, createdAt`，`type?: 'post'`
- `ArticleData`：`articleId, title, content, coverImage?, images[], author, createdAt`，`type: 'article'`
- 统一别名：`ContentData = PostData | ArticleData`

### 3.4 源码地图

```text
src/
├── background/
│   ├── serviceWorker.ts      # 入口：菜单、消息、图标监听
│   ├── iconClickHandler.ts   # 图标点击主流程
│   ├── pageUtils.ts          # 向 tab 要数据、剪贴板
│   └── toastUtils.ts
├── content-script/
│   ├── contentScript.ts      # 消息分发
│   ├── postExtractor.ts
│   ├── articleExtractor.ts
│   └── contentPreview.ts
├── common/
│   ├── types.ts / errors.ts / storage.ts
│   ├── apiClient.ts / serialization.ts
│   ├── markdownFormatter.ts / obsidianPayload.ts
│   ├── urlParser.ts / toast.ts
├── popup/                    # 有 default_popup 时的 UI（manifest 当前为图标直点）
└── options/
```

说明：当前 `manifest.json` 的 `action` **未配置** `default_popup`，因此工具栏图标走 `chrome.action.onClicked`（`iconClickHandler`）。Popup 源码仍在，可通过改 manifest 或扩展页入口使用；右键菜单与消息保存逻辑仍有效。

---

## 四、演进简史（根据文档日期与代码推断）

| 阶段 | 大致时间 | 特征 |
|------|----------|------|
| 立项 Prompt | ~2025-12 | `1.md`：只要远程 API + Popup + 右键 |
| 初版 README | 同期 | 只描述远程保存，未提剪贴板/Obsidian/长文章 |
| 长文章需求 | ~2026-01 | `X长文章.md` → 代码已落地 |
| 本地笔记 API 文档 | ~2026-01-22 | `27123` 旁路服务约定 |
| Obsidian 链路 | ~2026-06 | `obsidianPayload`、选项开关、安装指南、同级 Rust 服务 |
| 文档整理 | 2026-08-12 | 本文 + `docs/` 分层（见第六节） |

早期 MVP 设想（父目录 `x-likes-to-obsidian-mvp.md`）偏「Likes 列表批量 + Ollama」；**当前实现**是「单页详情/文章一键导入 + DeepSeek」，以代码与 `x-likes-to-obsidian/README.md` 为准。

---

## 五、整理前文档问题（为何乱）

| 原文件 | 问题 |
|--------|------|
| `README.md` | 过时：缺 Obsidian、剪贴板、长文章；克隆路径名不一致 |
| `1.md` | 原始 AI 开发 Prompt，不是用户文档，却与正式文档平铺根目录 |
| `X长文章.md` | 已实现的需求规格，未标状态，像待办 |
| `API.md` | 描述 **27123** 服务，易与主链路 **18787** 混淆 |
| `安装指南.md` | 内容最全且较新，但与过时 README 并列，读者不知先看谁 |

根因：**历史草稿、外部接口、操作手册、产品入口混在同一层**，且无「现行 / 归档」标记。

---

## 六、本次整理结果

```text
ddcherry-twitter/
├── README.md                          # 现行入口（已重写）
├── 项目梳理与文档规范.md              # 本文：梳理 + 原则
├── docs/
│   ├── 安装指南.md                    # 现行：扩展 + Rust 服务安装
│   ├── 接口/
│   │   └── 本地笔记服务-API.md        # 旁路 27123（原 API.md）
│   └── 归档/
│       ├── 原始需求-插件开发.md       # 原 1.md
│       └── 需求-X长文章.md            # 原 X长文章.md
├── src/ … public/ dist/ …
```

归档与接口文档文首均加了**状态说明**（现行 / 归档 / 旁路）。

---

## 七、文档整理原则（约定）

以下原则适用于本仓库后续写文档、挪文档；也作为同类小工具仓库的默认习惯。

### 原则 1：入口唯一、分层清晰

| 层级 | 放什么 | 本仓库对应 |
|------|--------|------------|
| 根目录 | 产品入口 + 总览类 1～2 份 | `README.md`、`项目梳理与文档规范.md` |
| `docs/` 现行 | 安装、运维、对接说明 | `安装指南.md`、`接口/*` |
| `docs/归档/` | 已落地或作废的需求/Prompt | 带状态条的历史稿 |

根目录**不**再堆临时笔记、编号文件（如 `1.md`）。

### 原则 2：名副其实

- 文件名用**内容语义**，不用序号：`原始需求-插件开发.md` 而非 `1.md`。
- 中文仓库、个人维护 → **中文文件名优先**；代码标识符仍英文。
- 接口文档写清**服务名 + 端口**，避免只写 `API.md`。

### 原则 3：单一事实源（Single Source of Truth）

| 信息类型 | 权威来源 | 其它处只做 |
|----------|----------|------------|
| 如何安装整套 | `docs/安装指南.md` | README 链过去 |
| 扩展当前能力与结构 | `README.md` + 本文 + **源码** | 归档需求不得覆盖 |
| 18787 服务配置 | `../x-likes-to-obsidian` 的 README / config | 安装指南可复述要点 |
| 27123 请求格式 | `docs/接口/本地笔记服务-API.md` | Popup 常量对齐文档 |

禁止在多处各写一套完整步骤却互不更新；重复处用链接。

### 原则 4：文首标注状态

每份非入口文档建议文首 3～5 行：

- **文档状态**：现行 / 归档 / 草案 / 旁路接口  
- **实现状态**（若是需求）：未做 / 部分 / 已落地  
- **读者与用途**：给谁看、解决什么

读者 10 秒内判断「还要不要当真理读」。

### 原则 5：以代码为准，文档跟代码走

- X 的 DOM 常变：提取逻辑以 `postExtractor` / `articleExtractor` 为准。
- 行为变更（新出口、改默认端口、改 payload）→ **先改代码，再改 README/安装指南**，并视情况在本文「演进」补一行。
- 归档需求**不回写**为现行规格，除非重新评审并移出 `归档/`。

### 原则 6：区分「本仓库」与「系统」

- 只谈扩展：写在本仓库 README。  
- 扩展 + Rust + Obsidian + DeepSeek：写在 `docs/安装指南.md`，并点名兄弟仓库路径。  
- 不要把兄弟仓库的完整运维粘成第二份却不维护。

### 原则 7：端口与链路写死对照表

文档里凡出现本地服务，必须带端口与路径，例如：

| 端口 | 路径示例 | 用途 |
|------|----------|------|
| 18787 | `/api/health`, `/api/tweets/import` | Obsidian 主链路 |
| 27123 | `/api/note` | 旁路本地笔记 |

避免只写「本地服务」导致装错、调错。

### 原则 8：少而精，能删则删

- 能合并进 README 的短说明不单独建文件。  
- 一次性对话 Prompt（如原始 `1.md`）实现后只归档，不继续改。  
- `node_modules`、构建日志、截图草稿不进文档树。

### 原则 9：改文档也要可检索

- 重要结论用表格或清单，少用长叙事。  
- 链接用相对路径，方便离线打开。  
- 重大整理在本文或提交说明里留日期（如本次 2026-08-12）。

---

## 八、日常怎么用（给未来的自己）

1. **想起来这是啥** → 读本文第一～三节 + `README.md`  
2. **换电脑重装** → `docs/安装指南.md`  
3. **改提取逻辑** → `src/content-script/*`，DOM 挂了优先查这里  
4. **Obsidian 没写入** → 查 `enableObsidian`、18787 是否启动、`x-likes-to-obsidian` 的 `config.toml`  
5. **加新文档** → 现行放 `docs/`，过期移 `docs/归档/` 并加状态条；更新 README 文档索引表  

---

## 九、已知缺口 / 注意点

1. **README 与 Popup**：manifest 未挂 `default_popup` 时，日常以图标点击为准；Popup 代码保留但默认可能进不去，属产品形态选择，不是半成品必删。  
2. **双本地端口**：18787 与 27123 并存，文档与选项文案需持续区分。  
3. **安装指南偏 Windows**：PowerShell / `.exe` / Windows 服务；在 macOS 上需自行把路径与启动方式替换为 `cargo run` 等（兄弟仓库 README 更贴近跨平台命令）。  
4. **父目录 MVP 文档**（`x-likes-to-obsidian-mvp.md`）与当前实现不完全一致，读时注意日期与技术选型（Ollama vs DeepSeek）。  
5. **X 前端变更风险**：无官方 API 时解析可能突然失败，需预留改选择器的时间。

---

## 十、原则速查（可贴到其它项目）

1. 根目录只留入口，细节进 `docs/`  
2. 归档与现行物理分开，文首标状态  
3. 文件名语义化，忌 `1.md`  
4. 同一事实只维护一处，其余链接  
5. 代码是行为的真理，文档跟版走  
6. 本仓库边界写清，系统级说明单独成篇  
7. 本地服务必写端口与路径  
8. 文档宜少、宜新、宜能删  

---

*本文随仓库维护；下次大改行为或文档结构时，请更新日期并补一节演进记录。*
